import { MediaManager } from "@/components/media/MediaManager";

export default function CounsellingMediaPage() {
  return (
    <MediaManager
      section="GALLERY"
      mediaGroup="COUNSELLING"
      title="Counselling Gallery"
      description="Manage photos and videos displayed in the Counselling gallery."
    />
  );
}
