import { MediaManager } from "@/components/media/MediaManager";

export default function WellnessMediaPage() {
  return (
    <MediaManager
      section="WELLNESS"
      title="Wellness Media"
      description="Manage background loops, meditative files, and custom session graphics shown inside the Wellness section."
    />
  );
}
