import { MediaManager } from "@/components/media/MediaManager";

export default function WorkshopsMediaPage() {
  return (
    <MediaManager
      section="WORKSHOPS"
      title="Mindset Workshops Gallery"
      description="Manage highlights (images/videos) and audience segments displayed inside the Mindset Workshops area."
    />
  );
}
