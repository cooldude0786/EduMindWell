import { MediaManager } from "@/components/media/MediaManager";

export default function InstitutionMediaPage() {
  return (
    <MediaManager
      section="GALLERY"
      mediaGroup="INSTITUTIONS"
      title="Institution Logos"
      description="Upload the logos shown in the homepage proof-of-work slider."
    />
  );
}