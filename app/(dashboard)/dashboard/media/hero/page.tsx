import { MediaManager } from "@/components/media/MediaManager";

export default function HeroMediaPage() {
  return (
    <MediaManager
      section="HERO"
      title="Hero Section Media"
      description="Manage the main video or image showcase on the home page's Hero Section."
    />
  );
}
